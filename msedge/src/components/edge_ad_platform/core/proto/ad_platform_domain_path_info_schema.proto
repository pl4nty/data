syntax = "proto3";

// Generates fast accessor methods but leaves out reflection.
option optimize_for = LITE_RUNTIME;

package ad_platform_domain_path_info_schema;

message DomainExperimentMapping {
	map<string, string> result = 1;
}
