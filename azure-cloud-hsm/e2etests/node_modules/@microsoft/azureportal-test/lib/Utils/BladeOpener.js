"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const Logger_1 = require("../Logger");
const Portal_1 = require("../Portal");
class BladeOpener {
    /**
     * Opens a SampleExtension blade with specified blade title
     *
     * @param {string} bladeTitle The title of the SampleExtension blade
     * @param {string} uriFragment The uri fragment of the Sample Extension blade (i.e. #blade/SamplesExtension/HelloFragment -> HelloFragment)
     * @param {number} [timeout] Optional time to wait, in milliseconds, for the blade to show up.
     * @param {boolean} [waitForBladeLoadedOnly] If true waits for the blade only to load, but not all of its tiles, before resolving
     * @returns A promise that resolves with the SampleExtension blade
     */
    static openSamplesExtensionBlade(bladeTitle, uriFragment, bladeType, timeout, waitForBladeLoadedOnly) {
        const blade = Portal_1.portal.blade({ title: bladeTitle, bladeType: bladeType });
        Logger_1.default.information(`Opening SampleExtension: ${bladeTitle} blade`);
        return Portal_1.portal
            .navigateToUriFragment(`blade/SamplesExtension/${uriFragment}`, timeout)
            .then(() => Portal_1.portal.getBladeLoadPromise(blade, waitForBladeLoadedOnly, timeout))
            .then(() => blade);
    }
}
exports.default = BladeOpener;
//# sourceMappingURL=BladeOpener.js.map