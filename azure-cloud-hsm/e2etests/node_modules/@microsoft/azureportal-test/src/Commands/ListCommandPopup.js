"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const By_1 = require("../Locators/By");
const PortalElement_1 = require("../PortalElement");
/**
 * Represents a list popup command in a blade's command bar.
 */
class ListCommandPopup extends PortalElement_1.default {
    constructor() {
        super(...arguments);
        /**
         * Represents the collection of items in the popup.
         */
        this.listItems = this.all(ListPopupItem);
    }
    /**
     * Gets the locator associated to this element.
     *
     * @returns A Locator instance.
     */
    get locator() {
        return By_1.default.className("fxs-commandBar-listpopup");
    }
    /**
     * Selects an item by it's text from list popup.
     *
     * @param {string} text The item text.
     * @returns  A promise that resolves when the item has been selected.
     */
    async selectItemByText(text) {
        const items = await this.listItems.filter(async (item) => {
            const itemText = await item.getText();
            return text === itemText;
        });
        if (items.length > 0) {
            return await items[0].click();
        }
        return null;
    }
}
exports.default = ListCommandPopup;
/**
 * Represents an item in a List popup.
 */
class ListPopupItem extends PortalElement_1.default {
    /**
     * Gets the locator associated to this element.
     *
     * @returns A Locator instance.
     */
    get locator() {
        return By_1.default.className("fxs-listpopup-item");
    }
}
//# sourceMappingURL=ListCommandPopup.js.map